version: '2'

services:
  database:
    image: 'docker.io/bitnami/mariadb:10.2-debian-10'
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - MARIADB_USER=admin
      - MARIADB_DATABASE=pandora_api
      - MARIADB_PASSWORD=pandora_33klwZ_76
    ports:
      - 3307:3306
    volumes:
      - mydb-db:/var/lib/mysql
  pandora:
    tty: true
    build:
      context: ./docker-image/
      dockerfile: Dockerfile
    environment:
      - DB_HOST=database
      - DB_USERNAME=admin
      - DB_DATABASE=pandora_api
      - DB_PASSWORD=pandora_33klwZ_76
      - APP_DEBUG=true
      - APP_ENV=local
      - APP_KEY=base64:gpy5yuVmMykxrpIqfovUFu84rcdFJMAYEyoFs/xxyuo=
      - DB_HOST_GP=SRVWMSTEST
      - DB_USERNAME_GP=PANDORA
      - DB_DATABASE_GP=Pandora
      - DB_PASSWORD_GP=%colseiN20*
    depends_on:
      - database
    ports:
      - 9002:3000
    volumes:
      - ./:/app
    # privileged: true # Privileged mode could be required to run this container under Windows
    # 
volumes:
  mydb-db: